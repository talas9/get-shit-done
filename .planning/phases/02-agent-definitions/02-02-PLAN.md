---
phase: 02-agent-definitions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/gsd-partitioner.md
autonomous: true
requirements:
  - PART-01
  - PART-02
  - PART-03
  - PART-04

must_haves:
  truths:
    - "gsd-partitioner.md exists as an L3 agent with Read and Bash tools"
    - "Partitioner accepts a phase directory path and calls gsd-tools.cjs hierarchy-partition"
    - "Partitioner returns the raw JSON partition map without modification"
    - "Partition output respects wave ordering — cross-wave plans are never in the same parallel stream"
  artifacts:
    - path: "agents/gsd-partitioner.md"
      provides: "L3 partitioner agent persona"
      contains: "tools: Read, Bash"
  key_links:
    - from: "agents/gsd-partitioner.md"
      to: "bin/lib/hierarchy.cjs"
      via: "CLI call to gsd-tools.cjs hierarchy-partition"
      pattern: "hierarchy-partition"
---

<objective>
Create the L3 partitioner agent definition.

Purpose: The partitioner is an L3 agent spawned by L1 before L2 dispatch. It analyzes a phase's plans for dependencies and file overlap, then returns a partition map assigning each plan to a named stream. It is intentionally a thin wrapper around `gsd-tools.cjs hierarchy-partition` (Phase 1 deliverable) — it does NOT reimplement partition logic.

Output: `agents/gsd-partitioner.md` agent file passing all frontmatter tests.
</objective>

<execution_context>
@/Users/talas9/.claude/get-shit-done/workflows/execute-plan.md
@/Users/talas9/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-agent-definitions/02-RESEARCH.md

<interfaces>
<!-- hierarchy-partition CLI interface (from Phase 1 bin/lib/hierarchy.cjs): -->
<!-- Command: node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" hierarchy-partition <phase_dir> -->
<!-- Output JSON schema: -->
```json
{
  "streams": [
    {
      "name": "stream-a",
      "plans": ["02-01-PLAN.md", "02-02-PLAN.md"],
      "worktree_branch": null
    }
  ]
}
```
<!-- worktree_branch is always null at partition time — set later by worktree-create during dispatch -->

<!-- Existing agent frontmatter pattern (read-only, no Write): -->
```yaml
---
name: gsd-integration-checker
description: Verifies cross-phase integration...
tools: Read, Bash, Grep, Glob
color: blue
skills: []
---
```
<!-- No hooks or anti-heredoc needed for non-Write agents -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create L3 partitioner agent definition</name>
  <files>agents/gsd-partitioner.md</files>
  <action>
Create `agents/gsd-partitioner.md` with the following structure:

**Frontmatter (exact):**
```yaml
---
name: gsd-partitioner
description: Analyzes a phase's plan list and returns a partition map assigning each plan to a named stream. Spawned by /gsd:execute-phase L1 orchestrator before L2 dispatch.
tools: Read, Bash
color: green
skills:
  - gsd-partitioner-workflow
---
```

Key frontmatter points:
- `tools: Read, Bash` — needs Read to inspect plan files if needed, Bash to call gsd-tools.cjs. No Write (output returned to caller, not written to file).
- `color: green` — analysis role
- No `# hooks:` block needed — no Write tool
- No `mcpServers` field needed — partitioner is a short-lived L3, MCP isolation is not a concern at this tier
- No anti-heredoc instruction needed — no Write tool

**Body structure — include these sections:**

1. `<role>` — L3 partitioner identity. Analyzes phase plans for dependency and file-overlap conflicts, groups them into non-conflicting parallel streams. Spawned by L1 orchestrator before L2 dispatch. This is a thin wrapper around `gsd-tools.cjs hierarchy-partition` — do NOT reimplement partition logic.

2. `<execution>` — Single-step execution flow:
   a. Extract the phase directory path from the `<phase_dir>` tag in the prompt
   b. Run via Bash: `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" hierarchy-partition <phase_dir>`
   c. If the command succeeds, return the raw JSON output directly as the agent's response — do NOT modify, filter, or reformat it
   d. If the command fails (non-zero exit), return the error message so L1 can handle it (likely fall back to flat execution)

3. `<output_schema>` — Document what L1 should expect:
   ```json
   {
     "streams": [
       {
         "name": "stream-a",
         "plans": ["XX-NN-PLAN.md", ...],
         "worktree_branch": null
       }
     ]
   }
   ```
   - `name`: Stream identifier (letter-based: stream-a, stream-b, etc.)
   - `plans`: Array of plan filenames assigned to this stream, in execution order
   - `worktree_branch`: Always null at partition time — L1 sets this when creating worktrees
   - Streams respect wave ordering: plans in different dependency waves are never in the same stream unless they have no cross-dependencies
   - The `max_l2_agents` config value caps the number of streams (enforced by the CLI tool, not the agent)

4. `<constraints>` — Explicit restrictions:
   - NEVER reimplement partition logic — call the CLI tool
   - NEVER modify the JSON output from hierarchy-partition
   - NEVER write files — return output as text to the caller
   - This agent is short-lived: one call, one response, done
  </action>
  <verify>
    <automated>cd /Users/talas9/Projects/get-shit-done && npm test 2>&1 | grep -E "FAIL|pass|fail|agent-frontmatter"</automated>
  </verify>
  <done>agents/gsd-partitioner.md exists, has correct frontmatter (tools: Read, Bash; color: green; skills: gsd-partitioner-workflow), body delegates to gsd-tools.cjs hierarchy-partition and returns raw JSON. All agent-frontmatter tests pass.</done>
</task>

</tasks>

<verification>
1. `npm test` passes with zero failures
2. `agents/gsd-partitioner.md` frontmatter contains exactly `tools: Read, Bash`
3. Agent body calls `gsd-tools.cjs hierarchy-partition` (not reimplementation)
4. Agent body returns raw JSON without modification
5. Output schema documented for Phase 3 consumers
</verification>

<success_criteria>
- gsd-partitioner.md passes all agent-frontmatter tests
- Agent is a thin CLI wrapper, not a reimplementation of partition logic
- Output schema is documented so Phase 3 dispatch knows what to parse
</success_criteria>

<output>
After completion, create `.planning/phases/02-agent-definitions/02-02-SUMMARY.md`
</output>

---
phase: 04-validation-and-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/hierarchy.test.cjs
  - .planning/phases/04-e2e-fixture/04e-01-PLAN.md
  - .planning/phases/04-e2e-fixture/04e-02-PLAN.md
autonomous: true
requirements:
  - VALID-04

must_haves:
  truths:
    - "npm test passes with new CLI return-shape assertions added"
    - "cmdWorktreeCreate return value includes { created, stream, branch, path }"
    - "cmdWorktreeRemove return value includes { removed, stream }"
    - "cmdStateReconcile return value includes { merged, worktrees_merged }"
  artifacts:
    - path: "tests/hierarchy.test.cjs"
      provides: "3 new unit tests for CLI return-value shapes"
      contains: "cmdWorktreeCreate.*return|cmdWorktreeRemove.*return|cmdStateReconcile.*return"
    - path: ".planning/phases/04-e2e-fixture/04e-01-PLAN.md"
      provides: "Fixture plan A touching scratch-a.txt only"
      contains: "scratch-a.txt"
    - path: ".planning/phases/04-e2e-fixture/04e-02-PLAN.md"
      provides: "Fixture plan B touching scratch-b.txt only"
      contains: "scratch-b.txt"
  key_links:
    - from: "tests/hierarchy.test.cjs"
      to: "bin/gsd-tools.cjs"
      via: "imports cmdWorktreeCreate, cmdWorktreeRemove, cmdStateReconcile"
      pattern: "require.*hierarchy"
---

<objective>
Add missing unit tests for CLI return-value shapes (VALID-04 gaps) and create the E2E test fixture plans for runtime validation.

Purpose: Close the 3 unit-test gaps identified in research (worktree-create, worktree-remove, state-reconcile return shapes) and scaffold the fixture phase that Plan 02 will use for live E2E verification.
Output: Green test suite with new assertions + 2 fixture plan files ready for execution.
</objective>

<execution_context>
@/Users/talas9/.claude/get-shit-done/workflows/execute-plan.md
@/Users/talas9/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-validation-and-hardening/04-RESEARCH.md
@tests/hierarchy.test.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CLI return-value shape unit tests</name>
  <files>tests/hierarchy.test.cjs</files>
  <behavior>
    - Test 1: cmdWorktreeCreate returns object with { created: true, stream: string, branch: string, path: string } on success
    - Test 2: cmdWorktreeRemove returns object with { removed: true, stream: string } on successful removal
    - Test 3: cmdStateReconcile returns object with { merged: true, worktrees_merged: number } on successful reconcile
  </behavior>
  <action>
    Add 3 new tests to the existing `tests/hierarchy.test.cjs` file. Follow the established test patterns already in that file (uses node:test, creates temp dirs, calls cmd functions directly). Each test:
    1. Sets up a temp directory with the required fixture state
    2. Calls the CLI function
    3. Asserts the return object has the expected shape and values

    For cmdStateReconcile, create a minimal scenario: create a worktree, write a modified STATE.md in it, then reconcile and verify the return shape.

    Reference the existing test patterns in hierarchy.test.cjs for setup/teardown. Do NOT mock anything — use real temp directories and real git operations as the existing tests do.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -E "hierarchy|FAIL|pass|fail"</automated>
  </verify>
  <done>All 3 new tests pass. Full npm test suite still green (no regressions).</done>
</task>

<task type="auto">
  <name>Task 2: Create E2E fixture phase with 2 non-overlapping plans</name>
  <files>.planning/phases/04-e2e-fixture/04e-01-PLAN.md, .planning/phases/04-e2e-fixture/04e-02-PLAN.md</files>
  <action>
    Create directory `.planning/phases/04-e2e-fixture/` and two minimal fixture plan files.

    Each plan must:
    - Have valid GSD frontmatter (phase, plan, type, wave, depends_on, files_modified, autonomous, must_haves)
    - Be wave 1 with no dependencies (so partitioner can assign them to separate streams)
    - Touch ONLY files inside `.planning/phases/04-e2e-fixture/` (sandbox — no real project files)
    - Have non-overlapping files_modified (plan 01: scratch-a.txt, plan 02: scratch-b.txt)
    - Include a trivial task that creates the scratch file with a marker string (e.g., "Plan 01 executed successfully")
    - Include objective, execution_context, tasks, verification, success_criteria, and output sections

    04e-01-PLAN.md: files_modified = [".planning/phases/04-e2e-fixture/scratch-a.txt"], single auto task that writes "Plan A complete" to scratch-a.txt
    04e-02-PLAN.md: files_modified = [".planning/phases/04-e2e-fixture/scratch-b.txt"], single auto task that writes "Plan B complete" to scratch-b.txt

    Both plans must produce a SUMMARY.md when executed (standard GSD behavior) so L2 completion checks pass.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const a=fs.readFileSync('.planning/phases/04-e2e-fixture/04e-01-PLAN.md','utf8'); const b=fs.readFileSync('.planning/phases/04-e2e-fixture/04e-02-PLAN.md','utf8'); const ya=a.match(/files_modified:[^]*/); const yb=b.match(/files_modified:[^]*/); console.log('Plan A exists:', a.includes('scratch-a')); console.log('Plan B exists:', b.includes('scratch-b')); console.log('No overlap:', !a.includes('scratch-b') && !b.includes('scratch-a'));"</automated>
  </verify>
  <done>Two fixture plan files exist with valid frontmatter, non-overlapping files_modified, wave 1, and sandboxed scratch files only.</done>
</task>

</tasks>

<verification>
- `npm test` passes with all existing + 3 new assertions
- `.planning/phases/04-e2e-fixture/` contains exactly 2 plan files
- Fixture plans have non-overlapping files_modified
- No fixture plan references files outside `.planning/phases/04-e2e-fixture/`
</verification>

<success_criteria>
- Full test suite green (554+ tests pass)
- 3 new return-shape tests cover cmdWorktreeCreate, cmdWorktreeRemove, cmdStateReconcile
- Fixture plans ready for E2E execution in Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/04-validation-and-hardening/04-01-SUMMARY.md`
</output>

---
phase: 04-validation-and-hardening
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - .planning/phases/04-validation-and-hardening/04-E2E-CHECKLIST.md
autonomous: false
requirements:
  - VALID-01
  - VALID-02
  - VALID-03

must_haves:
  truths:
    - "L2 standalone subagent successfully calls Agent() to spawn L3 executor"
    - "Hierarchy-enabled run produces structurally equivalent SUMMARYs to flat run"
    - "No orphaned worktrees remain after hierarchy run completes"
    - "Worktree registry is empty after cleanup"
  artifacts:
    - path: ".planning/phases/04-validation-and-hardening/04-E2E-CHECKLIST.md"
      provides: "Documented E2E test results with pass/fail for each check"
      contains: "VALID-01|VALID-02|VALID-03"
  key_links:
    - from: ".planning/phases/04-e2e-fixture/04e-01-PLAN.md"
      to: "execute-phase workflow"
      via: "gsd:execute-phase 04-e2e-fixture"
      pattern: "hierarchy_dispatch"
    - from: ".planning/worktree-registry.json"
      to: "git worktree list"
      via: "cross-reference for orphan detection"
      pattern: "worktrees.*\\[\\]"
---

<objective>
Execute the E2E fixture phase under hierarchy mode and verify VALID-01, VALID-02, VALID-03 with a human checkpoint.

Purpose: Prove the full L1->L2->L3 execution chain works at runtime — the fixture plans from Plan 01 are executed with hierarchy.enabled=true, and all runtime behaviors are verified.
Output: Completed E2E checklist documenting pass/fail for each validation requirement.
</objective>

<execution_context>
@/Users/talas9/.claude/get-shit-done/workflows/execute-plan.md
@/Users/talas9/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-validation-and-hardening/04-RESEARCH.md
@.planning/phases/04-validation-and-hardening/04-01-SUMMARY.md
@.planning/phases/04-e2e-fixture/04e-01-PLAN.md
@.planning/phases/04-e2e-fixture/04e-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute E2E test protocol and document results</name>
  <files>.planning/phases/04-validation-and-hardening/04-E2E-CHECKLIST.md</files>
  <action>
    Execute the full E2E validation protocol. Steps:

    **Pre-flight:**
    1. Confirm `npm test` passes (baseline green)
    2. Read current `.planning/config.json` and save original `hierarchy.enabled` value for restoration
    3. Set `hierarchy.enabled: true` in `.planning/config.json`

    **Hierarchy run:**
    4. Run `/gsd:execute-phase 04-e2e-fixture` (or equivalent: invoke the execute-phase workflow targeting the fixture phase)
       - NOTE: This step requires a live agent execution session. If direct workflow invocation is not possible from this context, document the manual steps and defer to the checkpoint.
    5. Observe partitioner output: must split 2 plans into 2 streams
    6. Observe L2 spawn: both L2s launched with run_in_background
    7. Observe L3 spawn: each L2 calls Agent() to spawn an L3 executor (VALID-01)
    8. Wait for completion

    **Post-run verification:**
    9. Check `git worktree list` — must show only main worktree (VALID-03)
    10. Check `.planning/worktree-registry.json` — must show `{ "worktrees": [] }` (VALID-03)
    11. Check fixture SUMMARY files exist for both plans (VALID-02 structural)
    12. Check scratch-a.txt and scratch-b.txt exist with expected content

    **Flat-mode comparison (VALID-02):**
    13. Set `hierarchy.enabled: false` in `.planning/config.json`
    14. Re-run the fixture phase in flat mode (or verify that the same structural output would be produced — same sections in SUMMARY, same files created)
    15. Compare SUMMARY structure (section headers, task completion status, files modified) — not byte-level content

    **Cleanup:**
    16. Restore `.planning/config.json` to original `hierarchy.enabled` value (should be false)
    17. Clean up fixture artifacts (scratch files, SUMMARYs) to leave repo clean

    **Document results:**
    Create `04-E2E-CHECKLIST.md` with a table of each check (steps 5-15), result (PASS/FAIL), and notes. Map each check to its VALID requirement ID.

    IMPORTANT: If live agent execution is not available in this context, document the checklist as a protocol to be run manually, marking results as PENDING, and defer to the checkpoint task for human verification.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-validation-and-hardening/04-E2E-CHECKLIST.md && echo "Checklist exists"</automated>
  </verify>
  <done>E2E checklist file exists with results (PASS/FAIL/PENDING) for all validation checks mapped to VALID-01, VALID-02, VALID-03.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of E2E results</name>
  <what-built>
    E2E test protocol executed (or documented for manual execution). The checklist covers:
    - VALID-01: L2 standalone subagent successfully spawns L3 via Agent()
    - VALID-02: Hierarchy run produces structurally equivalent output to flat run
    - VALID-03: No orphaned worktrees after completion; registry is clean
  </what-built>
  <how-to-verify>
    1. Read `.planning/phases/04-validation-and-hardening/04-E2E-CHECKLIST.md`
    2. If any checks are PENDING: execute them manually following the documented protocol
    3. Verify all VALID-01, VALID-02, VALID-03 checks show PASS
    4. Confirm `git worktree list` shows only the main worktree
    5. Confirm `.planning/config.json` has `hierarchy.enabled: false` (restored to default)
    6. If running the E2E manually: set hierarchy.enabled=true, run `/gsd:execute-phase 04-e2e-fixture`, verify partitioner splits into 2 streams, both L2s complete, worktrees cleaned up, then restore config
  </how-to-verify>
  <resume-signal>Type "approved" if all VALID checks pass, or describe which checks failed and why</resume-signal>
</task>

</tasks>

<verification>
- 04-E2E-CHECKLIST.md exists with results for all checks
- VALID-01: L2->L3 spawn confirmed working
- VALID-02: Hierarchy output structurally matches flat output
- VALID-03: git worktree list shows main only; registry empty
- Config restored to hierarchy.enabled: false
</verification>

<success_criteria>
- All VALID-01, VALID-02, VALID-03 checks PASS in the E2E checklist
- No orphaned worktrees or registry entries
- Config.json restored to pre-test state
- Human approves the checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/04-validation-and-hardening/04-02-SUMMARY.md`
</output>
